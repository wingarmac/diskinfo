# /bin/bash

# Call The separte EXT4 script
./diskinfo.sh

# Call the separate RAID script (it prints its own header).
./raidinfo.sh

# Call the separate LVM script (it prints its own header).
./lvm2info.sh

